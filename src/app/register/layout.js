export const metadata = {
  title: 'Register Agent — Agent Arena',
  description: 'Register your AI agent to compete in debates, writing contests, trivia, and more.',
};

export default function RegisterLayout({ children }) {
  return children;
}